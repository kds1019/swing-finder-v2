Fix GPT export crash when technical data is missing

- Fixed ValueError in build_trade_plan_for_gpt() when volume/RSI/EMA data is N/A
- Added safe type checking before formatting numeric values
- Volume now formats with commas only if numeric, otherwise shows "N/A"
- RSI, EMA20, EMA50 now format with decimals only if numeric
- Relative volume adds "x" suffix when numeric
- Fibonacci position formats with 1 decimal when numeric
- All GPT export prompts now work with complete OR missing data
- Added test_gpt_fix.py to verify all scenarios work

This fixes the crash reported in Active Trades when trying to copy trade data to GPT.

